/**
 * Default UI color for a newly-saved project layer. Matches the first entry
 * of the web-side LAYER_COLOR_PALETTE (src/web/components/open-project/layer-colors.ts).
 * Used by POST /api/projects/open when the client request omits the color field.
 */
export const DEFAULT_LAYER_COLOR = '#22c55e';
